name: CI

on:
  pull_request:
    branches: [main]

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - run: bun install --frozen-lockfile

      - name: Format check
        run: bunx oxfmt --check packages/ backend/ frontend/

      - name: Lint
        run: bunx oxlint --config .oxlintrc.json --type-check packages/ backend/ frontend/

      - name: Typecheck packages
        run: |
          bunx tsc --noEmit --project packages/schema/tsconfig.json
          bunx tsc --noEmit --project packages/dto/tsconfig.json
          bunx tsc --noEmit --project packages/mock/tsconfig.json

      - name: Typecheck backend
        run: bunx tsc --noEmit --project backend/tsconfig.json

      - name: Typecheck frontend
        run: bunx tsc --noEmit --project frontend/tsconfig.json

      - name: Dependency direction check
        run: bunx depcruise --config .dependency-cruiser.cjs backend/ frontend/ packages/

      - uses: hashicorp/setup-terraform@v3

      - name: Terraform format check
        run: terraform fmt -check -recursive infra/
