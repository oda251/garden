name: CI

on:
  pull_request:
    branches: [main]

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - run: bun install --frozen-lockfile

      - name: Format check
        run: bunx oxfmt --check packages/ backend/ frontend/

      - name: Lint
        run: bunx oxlint --config .oxlintrc.json --ignore-pattern 'frontend/.svelte-kit' packages/ backend/ frontend/

      - name: Typecheck packages
        run: |
          bunx tsc --noEmit --project packages/schema/tsconfig.json
          bunx tsc --noEmit --project packages/dto/tsconfig.json
          bunx tsc --noEmit --project packages/mock/tsconfig.json

      - name: Typecheck backend
        run: bunx tsc --noEmit --project backend/tsconfig.json

      - name: Typecheck frontend
        run: cd frontend && bunx svelte-kit sync && bunx svelte-check --tsconfig ./tsconfig.json

      - uses: hashicorp/setup-terraform@v3

      - name: Terraform format check
        run: terraform fmt -check -recursive infra/
